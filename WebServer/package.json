{
  "name": "webserver",
  "version": "1.0.0",
  "description": "This is the webserver that the HoloLens connects to. This allows other devices to communicate with the application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prompt": "^1.1.0",
    "ws": "^7.4.5"
  }
}
